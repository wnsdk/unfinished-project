apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "owners-user.fullname" . }}-test-connection"
  labels:
    {{- include "owners-user.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "owners-user.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
