apiVersion: v1
kind: Service
metadata:
  name: {{ include "owners-user.fullname" . }}
  labels:
    {{- include "owners-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "owners-user.selectorLabels" . | nindent 4 }}
